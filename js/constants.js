// body measures
const bodyLength = 9;
const bodyWidth = 4;
const bodyHeight = 5;

// back measures
const backLength = 5;
const backWidth = 2;
const backHeight = 5;

// head measure
const headEdgeLength = 2;

// antena measures
const antenaRadius = 0.25;
const antenaHeight = 1;

// eye measure
const eyeRadius = 0.25;

// arm measures
const armLength = 2;
const armWidth = 2;
const armHeight = 5;

// forearm measures
const forearmLength = 2;
const forearmWidth = 6;
const forearmHeight = 2;

// waist measure
const waistLength = 5;
const waistWidth = 4;
const waistHeight = 2;

// abdomen measures
const abdomenLength = 7;
const abdomenWidth = 4;
const abdomenHeight = 2;

// wheels measures  
const wheelRadius = 1.5;
const wheelHeight = 1.5;
const wheelOffset = 0.5;

// pipe measures
const pipeRadius = 0.25;
const pipeHeight = 6;

// upper leg measures
const upperLegLength = 1;
const upperLegWidth = 1;
const upperLegHeight = 3;

// lower leg measures
const lowerLegLength = 2;
const lowerLegWidth = 2;
const lowerLegHeight = 8;

// foot measures
const footLength = lowerLegLength + wheelHeight;
const footWidth = 2;
const footHeight = 1;

// trailer measures
const trailerLength = 9;
const trailerWidth = 16;
const trailerHeight = 9;

// trailer piece
const trailerPieceHeight = 1;
const trailerPieceRadius = 1;
const trailerOffset = 10;
const targetAxis_z = -23; 

// speed
const moveSpeed = 0.1;

// position
// const finalPosition = new THREE.Vector3(0, wheelRadius, 16);